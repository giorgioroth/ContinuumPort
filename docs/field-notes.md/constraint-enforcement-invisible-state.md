## Constraint Enforcement — Invisible State

A system may not fail.

It may enforce a constraint
on a state you cannot observe.

When execution is constrained by hidden state,
governance becomes indistinguishable
from blind execution.

— ContinuumPort
