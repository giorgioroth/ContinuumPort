## Execution Policy — Identity Boundary

Systems believe they ship features.

They do not.

They ship execution policy.

The layer that decides what state is allowed to persist
is the layer that defines what the system can become.

Admissibility is identity.

— ContinuumPort
